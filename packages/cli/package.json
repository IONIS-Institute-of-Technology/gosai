{
  "name": "@gosai/cli",
  "version": "0.1.0",
  "private": true,
  "description": "GOSAI command-line launcher (kiosk mode)",
  "type": "module",
  "main": "./src/index.ts",
  "bin": {
    "gosai": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
