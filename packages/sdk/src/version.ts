import { version } from '../package.json' with { type: 'json' };

/** Version of this SDK, e.g. `0.1.0`. Manifests name the versions they work with in `sdk`. */
export const SDK_VERSION: string = version;
