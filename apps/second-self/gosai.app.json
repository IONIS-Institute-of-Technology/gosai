{
  "$schema": "../../packages/shared/schemas/gosai.app.schema.json",
  "slug": "second-self",
  "name": "Second Self",
  "version": "1.0.0",
  "sdk": "^0.1.0",
  "description": "Augmented-mirror experience pack. A single composited surface projects interactive overlays onto a portrait mirror: skeleton overlays (hands, body, face), a gesture-driven launcher menu, motion games (poke-it, bounce, dance), audio experiences (theremine, music training) and sign-language modules (sign game, sign training). Driven by webcam pose estimation mirrored onto the display.",
  "author": "GOSAI",
  "default": "main",
  "requirements": { "display": true, "camera": true, "microphone": true, "speaker": true },
  "experiences": [
    {
      "slug": "main",
      "name": "Second Self",
      "description": "Composited augmented-mirror experience: persistent skeleton overlays plus a gesture menu that launches every Second Self module.",
      "entry": "dist/main.js",
      "drivers": ["pose", "pose_to_mirror", "frequency_analysis", "slr"]
    }
  ],
  "startup": ["main"],
  "settings": {
    "storageKey": "config",
    "groups": [
      {
        "label": "Projection",
        "description": "How the webcam frame maps into the experience. Reflection mode is calibrated on the mirror itself via the in-app wizard (menu > Calibrate).",
        "fields": [
          {
            "key": "projection.mode",
            "label": "Mode",
            "type": "select",
            "description": "Direct = webcam selfie overlay. Reflection = physical mirror rig (run the in-app calibration).",
            "default": "direct",
            "options": [
              { "value": "direct", "label": "Direct (webcam)" },
              { "value": "reflection", "label": "Reflection (mirror)" }
            ]
          },
          {
            "key": "projection.mirror",
            "label": "Mirror horizontally",
            "type": "boolean",
            "description": "Selfie-style left/right flip (direct mode).",
            "default": true
          }
        ]
      },
      {
        "label": "Sleep",
        "description": "Presence-based display sleep: the mirror fades to black when nobody stands in front of it and wakes when someone does.",
        "fields": [
          {
            "key": "sleep.enabled",
            "label": "Enabled",
            "type": "boolean",
            "description": "Fade the display to black while nobody is in front of the mirror.",
            "default": true
          },
          {
            "key": "sleep.wakeConfidence",
            "label": "Wake confidence",
            "type": "number",
            "description": "Smoothed presence confidence needed to wake the display (0 to 1).",
            "default": 0.6,
            "min": 0,
            "max": 1,
            "step": 0.05
          },
          {
            "key": "sleep.sleepConfidence",
            "label": "Sleep confidence",
            "type": "number",
            "description": "Below this smoothed confidence the user counts as absent (0 to 1).",
            "default": 0.35,
            "min": 0,
            "max": 1,
            "step": 0.05
          },
          {
            "key": "sleep.sleepDelaySec",
            "label": "Sleep delay (s)",
            "type": "number",
            "description": "Seconds of continuous absence before the display falls asleep.",
            "default": 7,
            "min": 0,
            "max": 3600,
            "step": 1
          },
          {
            "key": "sleep.maxDistanceM",
            "label": "Max distance (m)",
            "type": "number",
            "description": "People estimated farther than this (meters) are ignored.",
            "default": 2,
            "min": 0.3,
            "max": 20,
            "step": 0.1
          }
        ]
      }
    ]
  }
}
