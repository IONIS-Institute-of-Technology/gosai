{
  "name": "interactive-pool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts src/calibration.ts --target=browser --format=esm --outdir dist --external @gosai/sdk",
    "dev": "bun run build --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gosai/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
