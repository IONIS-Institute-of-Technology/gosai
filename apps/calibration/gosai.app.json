{
  "$schema": "../../packages/shared/schemas/gosai.app.schema.json",
  "slug": "calibration",
  "name": "Calibration",
  "version": "0.1.0",
  "description": "Runs the built-in calibration kinds, such as camera-projector-surface, for the app declaring them, in a control and a projector window.",
  "author": "GOSAI",
  "default": "calibrate",
  "requirements": { "display": true, "camera": true },
  "capabilities": ["calibration:write"],
  "experiences": [
    {
      "slug": "calibrate",
      "name": "Calibration Runner",
      "description": "Calibrates the target app. The windows open with role=control or role=projector and target=<app slug>; the drivers run under the target app's binding.",
      "entry": "dist/calibrate.js",
      "drivers": ["camera", "calibration"],
      "exclusive": true
    }
  ]
}
