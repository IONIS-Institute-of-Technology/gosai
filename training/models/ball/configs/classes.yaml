# Manual keep/drop decisions for source class names (case-insensitive).
# Anything not listed here is decided by the heuristic in
# src/gosai_train/pipelines/yolo_detect/sources.py (classify_name).
#
# `prepare` writes every discovered class name and its decision to
# classes.lock.yaml -- review that file after adding a dataset and promote any
# wrong decision here, e.g.:
#   overrides:
#     cue: drop
#     "mystery class": keep

overrides: {}
